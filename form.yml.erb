<%
  if ENV["OOD_DASHBOARD_TITLE"]=="Alvis OnDemand"
     cluster = "alvis1"
  else
     cluster = "vera3"
  end
%>
---
cluster:
  - "<%= cluster %>"
form:
  - desktop
attributes:
  desktop:
    widget: "select"
    options:
      - "xfce"
